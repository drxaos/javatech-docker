#Эта директива указывает базовый образ, который будет использоваться для создания Docker-образа.
#В данном случае используется официальный образ Nginx из Docker Hub.
FROM nginx

#Эта директива копирует файл конфигурации default.conf из текущего каталога (где находится Dockerfile) внутрь контейнера в каталог /etc/nginx/conf.d/.
#Файл default.conf обычно содержит настройки для веб-сервера Nginx, такие как прокси, маршрутизация и другие параметры конфигурации.
COPY ./default.conf /etc/nginx/conf.d/default.conf

#Эта директива копирует файл 50x.html из текущего каталога (где находится Dockerfile) внутрь контейнера в каталог /usr/share/nginx/html.
#Файл 50x.html часто используется для отображения страниц ошибок (например, ошибки сервера 500) на веб-сайте, обслуживаемом Nginx.
COPY ./50x.html /usr/share/nginx/html

#Эта директива определяет команду, которая будет выполнена при запуске контейнера. В данном случае, при запуске контейнера будет запущен процесс Nginx с указанными параметрами.
#Опция -g "daemon off;" указывает Nginx не запускать в режиме демона, а оставаться в переднем плане, что полезно при запуске Nginx в контейнере.
CMD ["nginx","-g","daemon off;"]
