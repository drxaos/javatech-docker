# установка образа и версия
FROM node:12
# контекст, в котором будет запускаться контейнер
WORKDIR /app
# говорит докеру, что данный контейнер использует порт 3000
EXPOSE 3000
# после запуска контейнера выполнить npm install и в случае успеха выполнить npm start
CMD npm install && npm start
