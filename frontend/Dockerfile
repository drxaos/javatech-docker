#Эта директива указывает базовый образ, который будет использоваться для создания Docker-образа.
#В данном случае используется образ node:12, который предварительно содержит Node.js и npm.
FROM node:12

#Эта директива устанавливает рабочий каталог внутри контейнера на /app. Это означает,
#что все последующие команды будут выполняться в этом каталоге.
WORKDIR /app

#Эта директива объявляет, что контейнер будет прослушивать порт 3000.
#Однако она не привязывает порт к какому-либо конкретному порту на хост-машине.
#Это лишь указание на то, что приложение внутри контейнера будет доступно на порту 3000.
EXPOSE 3000

#Эта директива определяет команду, которая будет выполнена при запуске контейнера.
#В данном случае используется комбинация команд npm install и npm start.
#Первая команда устанавливает зависимости приложения из файла package.json, а вторая запускает приложение.
CMD npm install && npm start
